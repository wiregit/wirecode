<?xml version="1.0" encoding="UTF-8"?>

<project name="lw-resources" basedir="." xmlns:ivy="antlib:org.apache.ivy.ant">
    <property name="component" value="resources"/>
    <import file="../common/generic-build.xml"/>
    
    <target name="jar" depends="initialize, compile-src">
        <ivy:resolve file="${limewire.components.resources.home}/ivy.xml"/>
        <do.jar component="${component}" basedir="${limewire.components.resources.src.resources}"/>
    </target>
</project>